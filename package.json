{
  "name": "node-typescript-lerna-vagrant-boilerplate",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start",
    "clean": "lerna run clean"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {
    "rimraf": "^3.0.2",
    "tsconfig-paths": "^3.9.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
